'use strict';

/**
 * shop-claim-request service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::shop-claim-request.shop-claim-request');
