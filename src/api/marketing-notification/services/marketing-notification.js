'use strict';

/**
 * marketing-notification service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::marketing-notification.marketing-notification');
